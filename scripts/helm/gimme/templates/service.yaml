apiVersion: v1
kind: Service
metadata:
  name: {{ include "gimme.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "gimme.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "gimme.selectorLabels" . | nindent 4 }}
